import type { Accessor, JSX } from "solid-js";

interface KeybindsProps {
  id: string;
  active: Accessor<string>;
}

export default function Keybinds(proops: KeybindsProps): JSX.Element {
  return <></>;
}
